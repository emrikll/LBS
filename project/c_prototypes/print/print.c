#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main(int argc, char *argv[]) {
    if (argc != 2) {
        printf("Usage: %s text", argv[0]);
        exit(0);
    }

    char buffer[1024];
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);
    strcpy(buffer, argv[1]);

    printf("You wrote: %s \n", buffer);
    exit(0);
}